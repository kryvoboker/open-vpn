port 1194
proto udp
dev tun

topology subnet
server 10.8.0.0 255.255.255.0

    # Full tunnel (bypassing blocks / accessing the Internet through a server)
push "redirect-gateway def1 bypass-dhcp"

    # DNS для клієнтів
push "dhcp-option DNS 1.1.1.1"
push "dhcp-option DNS 8.8.8.8"

keepalive 10 120
persist-key
persist-tun

    # PKI
ca   /etc/openvpn/pki/ca.crt
cert /etc/openvpn/pki/issued/server.crt
key  /etc/openvpn/pki/private/server.key
dh   /etc/openvpn/pki/dh.pem

    # TLS-crypt key
tls-crypt /etc/openvpn/pki/tls-crypt.key

    # Modern ciphers (OpenVPN 2.5+)
data-ciphers AES-256-GCM:AES-128-GCM:CHACHA20-POLY1305
data-ciphers-fallback AES-256-GCM
auth SHA256
tls-version-min 1.2

    # Logs
status /var/log/openvpn-status.log
verb 3

    # Process safety
user nobody
group nogroup

    # CRL (certificate revocation)
crl-verify /etc/openvpn/pki/crl.pem

# If you need access to Docker subnets/local networks, add a push route separately.
# push "route 172.18.0.0 255.255.0.0"